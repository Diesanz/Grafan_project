\documentclass{article}
\usepackage{graphicx}
\usepackage[spanish]{babel}
\usepackage{float}
\usepackage{minted}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage[backend=bibtex]{biblatex}
\addbibresource{bibliografia.bib}
\usepackage[a4paper, top=3cm, bottom=3cm, left=3.5cm, right=3.5cm]{geometry}

\lstset{
    language=bash,
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{blue},
    commentstyle=\color{gray},
    stringstyle=\color{green!60!black},
    backgroundcolor=\color{gray!10},
    frame=single,
    breaklines=true,
    captionpos=b,
    numbers=left,
    numberstyle=\tiny\color{gray}
}

\lstdefinelanguage{SQL}{
    keywords={SELECT, FROM, WHERE, INSERT, INTO, VALUES, UPDATE, SET, DELETE, CREATE, TABLE, ALTER, DROP, INDEX},
    keywordstyle=\color{blue}\bfseries,
    morekeywords={PRIMARY, KEY, FOREIGN, NOT, NULL, AUTO_INCREMENT, DEFAULT, CONSTRAINT},
    sensitive=false,
    morestring=[b]',
    morecomment=[l]--,
    morecomment=[s]{/*}{*/},
    commentstyle=\color{gray}\ttfamily,
    stringstyle=\color{red}\ttfamily,
}

\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} 
\fancyhead[L]{Práctica 1} 
\fancyhead[R]{ESI} 
\fancyfoot[C]{\thepage}

\begin{document}

% Portada
\begin{titlepage}
    \centering
    \includegraphics[width=0.4\textwidth]{logo.png} % Ajusta el nombre de la imagen según corresponda
    \\[2cm]
    {\Huge \textbf{Práctica 1: Grafana}}\\[1.5cm]
    {\Large \textit{Evaluación de Sistemas Informáticos}}\\[1cm]
    {\large 3º Grado en Informática. Mención de TI}\\[1cm]
    {\large Grupo 6}\\[1.5cm]
    \textbf{Autores:}\\[0.7cm]
    Víctor Fernández Sánchez\\
    Gonzalo Sánchez Maroto\\
    Diego Sanz Santos\\
    Rubén Verduras Cavada\\[2cm]
    \vfill
    \today
\end{titlepage}

\tableofcontents % Componer y mostar el índice
\listoffigures % Componer y mostrar el índice de tablas
\listoftables % Componer y mostrar el índice de figuras
\newpage

\section{¿Qué es Grafana?}

\textbf{Grafana} es un software de código abierto que permite consultar, visualizar, alertar y comprender datos en tiempo real sin importar dónde estén almacenados mostrados a través de paneles elegantes y flexibles.\\

Las principales \textbf{características} de Grafana son:

\begin{itemize}
    \item Permite crear \textbf{dashboards} altamente interactivos con gráficos y tablas que se pueden personalizar y explirar en tiempo real.
    \item Admite una amplia variedad de fuentes de datos como \textbf{bases de datos} \textit{SQL} y \textit{NoSQL}, sistemas de \textbf{monitoreo} como \textit{Prometheus} y \textit{Graphite} y sistemas en la \textbf{nube} como \textit{AWS CloudWatch} y \textit{Google Stackdriver}.
    \item Ofrece una amplia gama de opciones de \textbf{visualización} como gráficos de líneas, barras, dispersión, termómetros y más.
    \item Aporta la posibilidad de crear \textbf{alertas} basadas en umbrales y condiciones definidas por los usuario capaces de notificar a través de correo electrónico u otros canales de comunicación.
    \item Facilita la \textbf{exploración} y \textbf{análisis} de datos mediante funciones de búsqueda, filtrado y agregación, que permite a los usuarios investigar tendencias y patrones en los datos.
    \item Altamente seguro con sistemas basados en autenticación de usuarios y controles de acceso a datos basados en roles.
\end{itemize}

Según estas características algunos \textbf{casos de uso} son los siguientes:

\begin{enumerate}
    \item \textbf{Monitoreo} de infraestructura: supervisar el rendimiento de servidores, redes y bases de datos, identificando problemas y optimizando recursos.
    \item \textbf{Visualización} de métricas de aplicaciones: rastrea el rendimiento, identifica cuellos de botella y mejora la experiencia del usuario.
    \item \textbf{Análisis} de registros: correlacionar datos de registros con métricas para investigar problemas y tendencias en sistemas distribuidos.
    \item \textbf{Observabilidad} de microservicios: monitorizar la salud y rendimiento de estos para identificar problemas de comunicación entre servicios.
    \item \textbf{Seguridad} informática: visualizar datos de seguridad, como registros de acceso, eventos de autenticación y actividad sospechosa, para detectar y responder a las amenazas de manera más eficiente.
\end{enumerate}

\section{Instalación Grafana}

Para realizar la instalación de \textbf{Grafana} en su versión \textit{Standalone Linux Binaries}, se ejecutaron los siguientes comandos, que descargarán y descomprimirán el archivo respectivamente:\\

\begin{lstlisting}[language=bash]
wget https://dl.grafana.com/enterprise/release/grafana-enterprise-11.5.2.linux-amd64.tar.gz
tar -zxvf grafana-enterprise-11.5.2.linux-amd64.tar.gz
\end{lstlisting}

\vspace{0.4cm}
Una vez descomprimido todo se debe de \textbf{iniciar el servidor} ejecutando el 
comando \lstinline[language=bash]|systemctl start grafana-server.service| 
y podemos confirmar que se ha iniciado si ejecutamos \lstinline[language=bash]|systemctl status grafana-server.service|.\\

Con el servidor de Grafana en funcionamiento, se puede acceder a este mediante la \textbf{URL}  ''http://virtual.lab.inf.uva.es:31222'' (en nuestro caso).

\section{Base de datos}

\textbf{Grafana} necesita estar conectada a una base de datos para poder crear sus tablas y esquemas para mostrar los valores que se almacenan en ellas. Utilizaremos MySQL, en donde Grafana puede utilizarlo de dos maneras distintas:

\begin{itemize}
    \item Como base de datos interna de Grafana, para usuarios y permisos, dashboards y paneles o ajustes o auditorías.
    \item Como fuente de datos externa, en donde Grafana realizará consultas SQL a los datos y creará gráficos para mostrarlos, construyendo así dashboards dinámicos.
\end{itemize}

En nuestra práctica hemos conectado una base de datos MySQL llamada ''grafana'' con URL host ''localhost:3306'' debido a que 3306 es el puerto predeterminado de MySQL.

\subsection{Diagrama y estructura de la base de datos}
A continuación se muestra el diagrama que ilustra la estructura de la base de datos diseñada para la recolección de datos de rendimiento del servidor.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{DiagramaDB.png}
    \caption{Diagrama de la Base de Datos}
    \label{fig:diagrama-DB}
\end{figure}
\newpage
Para una mejor comprensión del esquema, se proporciona a continuación una breve descripción de cada tabla y los atributos que la componen:

\begin{enumerate}
    \item \textbf{CPU:} Tabla para almacenar información sobre el uso del procesador (CPU).
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{carga}: Porcentaje de uso de la CPU.
        \item \textbf{frecuencia}: Frecuencia de la CPU en GHz.
    \end{itemize}
    \item \textbf{Memoria:} Tabla para almacenar información sobre el uso de la memoria RAM y el espacio de intercambio (swap).
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{ram}: Cantidad de memoria RAM utilizada en GB.
        \item \textbf{swap}: Cantidad de espacio de intercambio utilizado en GB.
    \end{itemize}
    \item \textbf{Disco:} Tabla para almacenar información sobre el espacio utilizado en el disco.
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{espacio}: Cantidad de espacio en disco utilizado en GB.
    \end{itemize}
    \item \textbf{EntradaSalida:} Tabla para almacenar información sobre las operaciones de entrada/salida (E/S).
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{operaciones}: Número de operaciones de E/S por segundo.
    \end{itemize}
    \item \textbf{Procesos:} Tabla para almacenar información sobre los procesos en ejecución.
    \begin{itemize}
        \item \textbf{pid}: Identificador del proceso.
        \item \textbf{cpu\_percent}: Porcentaje de uso de la CPU por el proceso.
        \item \textbf{memory\_percent}: Porcentaje de uso de la memoria RAM por el proceso.
        \item \textbf{vsz}: Tamaño virtual del proceso en memoria.
        \item \textbf{rss}: Tamaño real del proceso en RAM.
        \item \textbf{usuario}: Usuario que ejecuta el proceso.
        \item \textbf{nombre}: Nombre del proceso.
        \item \textbf{ruta}: Ruta del ejecutable del proceso.
        \item \textbf{hilos}: Número de hilos del proceso.
        \item \textbf{estado}: Estado del proceso (''ejecutándose", ''inactivo", "durmiendo", etc).
    \end{itemize}
    \item \textbf{Num\_procesos:} Tabla para almacenar el número total de procesos en ejecución.
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{numero}: Número total de procesos en ejecución.
    \end{itemize}
    \item \textbf{Red:} Tabla para almacenar información sobre el tráfico de red.
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{bytes\_enviados}: Número de bytes enviados.
        \item \textbf{bytes\_recibidos}: Número de bytes recibidos.
        \item \textbf{conexiones\_activas}: Número de conexiones activas.
        \item \textbf{paquetes\_enviados}: Número de paquetes enviados.
        \item \textbf{paquetes\_recibidos}: Número de paquetes recibidos.
    \end{itemize}
    \item \textbf{Interfaces:} Tabla para almacenar información sobre las interfaces de red.
    \begin{itemize}
        \item \textbf{nombre}: Nombre de la interfaz.
        \item \textbf{direccion}: Dirección IP de la interfaz.
        \item \textbf{mac}: Dirección MAC de la interfaz.
        \item \textbf{broadcast}: Dirección de broadcast de la interfaz.
        \item \textbf{bytes\_enviados}: Número de bytes enviados por la interfaz.
        \item \textbf{bytes\_recibidos}: Número de bytes recibidos por la interfaz.
    \end{itemize}
    \item \textbf{Tiempo\_activo:} Tabla para almacenar el tiempo de actividad del servidor.
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{dias}: Número de días de actividad.
        \item \textbf{horas}: Número de horas de actividad.
        \item \textbf{minutos}: Número de minutos de actividad.
        \item \textbf{segundos}: Número de segundos de actividad.
    \end{itemize}
    \item \textbf{Sistema:} Tabla para almacenar información sobre la carga del sistema.
    \begin{itemize}
        \item \textbf{timestamp}: Marca de tiempo de la medición.
        \item \textbf{carga1}: Carga promedio del sistema en el último minuto.
        \item \textbf{carga5}: Carga promedio del sistema en los últimos 5 minutos.
        \item \textbf{carag15}: Carga promedio del sistema en los últimos 15 minutos.
        \item \textbf{nucleos\_log}: Número de núcleos lógicos de la CPU.
    \end{itemize}
\end{enumerate}

\subsection{Conexión a la base de datos}\label{subsec:conexión-db}
Este código define una conexión con la base de datos \textbf{MySQL} en Ubuntu. El script está desarrollado en Python y maneja tanto la conexión con la base de datos como su cierre.\\

\begin{lstlisting}[language=python]
def get_connection():
    try:
        """
        Establece una conexión con la base de datos MariaDB utilizando los parámetros proporcionados.
                
        return: Devuelve un objeto de tipo conexión a la base de datos si la conexión es exitosa.
                Si ocurre un error, devuelve None.
        """
        connection = pymysql.connect(
            host= "localhost",
            port= int(3306),
            database= "grafana",
            user= "grafanareader",
            password= "Grupo6esi",
            charset='utf8mb4',  # Evita problemas de encoding
            cursorclass=pymysql.cursors.DictCursor
        )
        return connection
    except pymysql.MySQLError as e:
        print("Error al conectarse a la base de datos:" + str(e))
        return None
\end{lstlisting}

\vspace{0.4cm}
Explicación:

\begin{itemize}
    \item \textbf{get\_connection()}: Establece una conexión con la base de datos MySQL. Para ello, se deben especificar el \textit{host, puerto, nombre de la base de datos, usuario y contraseña}. Retorna un objeto de conexión \textbf{pymysql.connections.Connection} si la conexión es exitosa; en caso contrario, retorna \textbf{None}.
    \item \textbf{close\_connection()}: Cierra una conexión existente con la base de datos. Requiere como parámetro un objeto de conexión \textbf{pymysql.connections.Connection} que será cerrado.
\end{itemize}

\subsection{Iniciar la base de datos en el servidor}

Para \textbf{iniciar} la base de datos en el servidor es necesario acceder como root con el comando \lstinline{sudo -i}. A continuación ejecutamos los siguientes comandos:

\begin{itemize}
    \item \lstinline{mysql} $ \rightarrow $ abre el cliente de línea de comandos de MySQL, permitiendo interactuar con la base de datos.
    \item \lstinline{use grafana} $ \rightarrow $ selecciona la base de datos llamada 'grafana' para que todas las operaciones posteriores se ejecuten sobre ella.
    \item \lstinline{source /home/usuario/esi/db/tablasGrafana.sql;} $ \rightarrow $ ejecuta el archivo SQL que contiene las instrucciones para crear las tablas y almacenar datos en la base de datos 'grafana'.
\end{itemize}

\subsection{Script de creación de tablas}

Lo primero que se puede ver en el script es el comando \lstinline{DROP TABLE IF EXISTS}, que tiene como objetivo asegurarse de que no existan tablas previas con los mismos nombres antes de crear unas nuevas. Especialmente tiene estas ventajas:

\begin{itemize}
    \item Evitar errores por duplicación.
    \item Facilitar la ejecución repetida.
    \item Asegurar un entorno limpio.
    \item Evitar dependencias inválidas.
\end{itemize}

El siguiente comando distinto es \lstinline{DROP PROCEDURE IF EXISTS}, que tiene una función similar a \lstinline{DROP TABLE}, pero aplicada a procedimientos almacenados.\\

Las tablas de la base de datos tiene en común que su \textbf{clave primaria} es el 'timestamp', que es una marca de tiempo que representa un momento específico. Debido a que no puede haber dos momentos de tiempo es un atributo ideal para ser la clave primaria de cada tabla.\\

Más allá de la clave primaria, en cada tabla se recogen distintos \textbf{tipos} de datos según lo que se quiere recopilar, como datos de tipo INT, FLOAT, VARCHAR(255), que todos estos tienen la peculiaridad de que no pueden ser un valor nulo.\\

Además, en este script se define el procedimiento 'AddDatos', cuya declaración es la siguiente:\\

\begin{lstlisting}[language=sql]
CREATE PROCEDURE `AddDatos` (
    IN `carga_cpu` FLOAT,
    IN `frecuencia_cpu` FLOAT,
    IN `mem_ram` FLOAT,
    IN `mem_swap` FLOAT,
    IN `espacio_disco` FLOAT,
    IN `io_operaciones` INT,
    IN `num_proc` INT,
    IN `bytes_env` INT,
    IN `bytes_rec` INT,
    IN `conexiones` INT,
    IN `paq_env` INT,
    IN `paq_rec` INT,
    IN `d` INT,
    IN `h` INT,
    IN `m` INT,
    IN `s` INT,
    IN `carga_sis_1` FLOAT,
    IN `carga_sis_5` FLOAT,
    IN `carga_sis_15` FLOAT,
    IN `nucleos_log` INT
)
\end{lstlisting}

\vspace{0.4cm}
El procedimiento acepta varios \textbf{parámetros de entrada} (IN) que representan los datos de rendimiento del servidor (excepto los valores correspondientes a las tablas interfaces y procesos).\\

Una vez recibidos estos parámetros, 'AddDatos' calcula la marca de tiempo actual que registrará en qué momento se han recopilado los datos. Además, define un controlador de errores que se activa si ocurre alguna excepción SQL durante la ejecución del procedimiento.\\

Posteriormente, comienza una \textbf{transacción} para asegurar que todas las operaciones de inserción se realicen con éxito o se reviertan en caso de error.\\

Comenzada la transacción, el procedimiento introduce los datos que ha recibido como parámetros en las tablas correspondientes (excepto en las tablas interfaces y procesos). También, antes de introducir los nuevos datos, borra los datos de las tablas  \textbf{CPU, Disco, Num\_procesos, Tiempo\_activo y Sistema} que estaban registrados anteriormente.\\

Finalmente, Si todas las operaciones se realizan con éxito, confirma la transacción (COMMIT), guardando los cambios en la base de datos.

\section{Servicio de Ubuntu}\label{sec:servicio-ubuntu}
Este código define un servicio de \textbf{systemd} de Ubuntu, que ejecuta un script desarrollado en python3 dedicado a la recolección de datos del servidor y enviarlos a Grafana. El servicio depende de MySQL y se reinicia automáticamente si falla.
Este código se encuentra dentro de \textbf{/etc/systemd/system/} con el nombre de \textbf{programa-grupo6.service}.
\\

\begin{lstlisting}[language=bash]
[Unit]
Description=Daemon programa esi
After=network.target
After=mysql.service
Requires=mysql.service

[Service]
ExecStart=/usr/bin/python3 /home/usuario/esi/script/script_grafana.py
WorkingDirectory=/home/usuario/esi
Restart=always
User=usuario
Group=usuario

[Install]
WantedBy=multi-user.target 
\end{lstlisting}

\vspace{0.4cm}
Explicación:

\begin{itemize}
    \item \textbf{[Unit]} $ \rightarrow $ define la descripción y dependencias del servicio. Indica que se ejecuta después de que la red y MySQL estén activos.
    \item \textbf{[Service]} $ \rightarrow $ especifica el comando para iniciar el script, su directorio de trabajo y que se reiniciará en caso de fallo. Además, se ejecutará con el usuario \textit{usuario}.
    \item \textbf{[Install]} $ \rightarrow $ indica que el servicio debe ejecutarse en el nivel \textit{multi-user.target}.
\end{itemize}

\section{Explicación del código}
\subsection{Bucle while del servicio}\label{subsec:bucle-while}

\begin{lstlisting}[language=python]
# Configuración de conexión a la base de datos
conn = None

try:
    conn = get_connection()  # Obtener la conexión a la base de datos

    if conn:
        print("Conexión a la base de datos establecida correctamente")
        # Obtener el tiempo que el servidor ha estado activo
        boot_time = psutil.boot_time()  # Tiempo de arranque, momento en el que arrancó el servidor

        # Ejecuta la recolección e inserción de datos en intervalos regulares
        while True:
            data = collect_data(boot_time)  # Llama a la función collect_data para recolectar los datos
            if data:
                insert_data(data, conn)  # Llama a la función insert_data para insertar los datos en la base de datos

            time.sleep(5)  # Espera 5 segundos antes de la siguiente medición
except Error as e:
    print("Error al conectar con la base de datos:" + str(e))
finally:
    if conn:  # Si la conexión fue exitosa, se cierra
        close_connection(conn)  # Cierra la conexión al finalizar el programa
\end{lstlisting}

\vspace{0.4cm}
Explicación:

\begin{itemize}
    \item \textbf{Establecimiento de conexión}: Se establece la conexión con la base de datos mediante el método \textbf{get-connection()}.
    \item \textbf{Comprobación de conexión}: Se verifica si la conexión fue exitosa.
    \item \textbf{Bucle while}: Este es un bucle infinito que estará activo mientras el servicio siga en ejecución. Dentro de este bucle:
    \begin{itemize}
        \item Se recopilan datos utilizando el método \textbf{collect-data()}.
        \item Se insertan los datos recogidos en la base de datos con el método \textbf{insert\_data()}, que se explica más adelante en la sección.
        \item Se espera 5 segundos antes de repetir el proceso.
    \end{itemize}
    \item \textbf{Manejo de errores}: Captura posibles errores de conexión con la base de datos.
    \item \textbf{Cierre de conexión}: Se cierra la conexión, si existe, dentro del bloque \texttt{finally} para evitar posibles fugas de recursos mediante el método \textbf{close\_connection(conn)}, que se explica más adelante en la sección.
\end{itemize}

\subsection{Recolección de datos}\label{subsec:recolección-datos}

Para la recolección de datos se ha utilizado una librería de Python llamada \textbf{psutil}, que se encarga de obtener información de los procesos y utilización del sistema como puede ser la cpu, memoria, discos, redes o sensores y que funciona tanto en Windows, MacOS o Linux.\\

A continuación se hará un listado de las métricas recogidas con la librería psutil:

\begin{enumerate}
    \item Carga instantánea de CPU $ \rightarrow $ \lstinline{psutil.cpu_percent(interval=1)}
    \item Frecuencia intantánea de CPU $ \rightarrow $ \lstinline{psutil.cpu_freq().current}
    \item Uso de memoria RAM y memoria SWAP $ \rightarrow $ \lstinline{psutil.virtual_memory().percent} y \lstinline{swap_memory().percent}
    \item Espacio en disco $ \rightarrow $ \lstinline{psutil.disk_usage('/').percent}
    \item Operaciones de E/S $ \rightarrow $ \lstinline{psutil.disk_io_counters().read_count}
    \item Número de bytes enviados y recibidos  $ \rightarrow $ \lstinline{psutil.net_io_counters().bytes_sent} y \lstinline{psutil.net_io_counters().bytes_recv}
    \item Número de conexiones de red activas si las hubiera  $ \rightarrow $ \lstinline{len(psutil.net_connections(kind='inet'))}
    \item Información detallada de los procesos en ejecución  $ \rightarrow $ para ello se creó una clase que recopilaba todos los detalles de los procesos:
    \begin{lstlisting}[language=python]
# Obtener detalles de los procesos
    proc_detail = [
        {
            "pid": proc.info["pid"],
            "name": proc.info["name"],
            "exe": proc.info["exe"] if proc.info["exe"] else "[{}]".format(proc.info["name"]),
            "status": proc.info["status"],
            "username": proc.info["username"] if proc.info["username"] else "N/A",
            "cpu_percent": proc.info["cpu_percent"],
            "memory_percent": proc.info["memory_percent"],
            "vsz": proc.memory_info().vms,
            "rss": proc.memory_info().rss,
            "num_thread": proc.num_threads()
        }
        for proc in psutil.process_iter(['pid', 'name', 'exe', 'status', 'username', 'cpu_percent', 'memory_percent'])
    ]
    \end{lstlisting}
    \item Momento en el que se arrancó el servidor $ \rightarrow $ \lstinline{psutil.boot_time()}
    \item Información detallada de las interfaces de red $ \rightarrow $ para ello se creó una clase que recopilaba todos los detalles de las distintas interfaces de red:
    \begin{lstlisting}[language=python]
interfaces_info = []
    for nombre_interfaz, interface_addreses in psutil.net_if_addrs().items():
            direccion_mac = None
            direccion_ipv4 = None
            subnet = None
            broadcast = None
            net_io = psutil.net_io_counters(pernic=True)

            for address in interface_addreses:
                if str(address.family) == 'AddressFamily.AF_INET':
                    direccion_ipv4 = address.address if address.address else "IPv4"
                    subnet = address.netmask if address.netmask else "N/A"
                    broadcast = address.broadcast if address.broadcast else "N/A"
                elif str(address.family) == 'AddressFamily.AF_PACKET':
                    direccion_mac = address.address if address.address else "MAC"

            bytes_enviados = net_io[nombre_interfaz].bytes_sent if nombre_interfaz in net_io else 0
            bytes_recibidos = net_io[nombre_interfaz].bytes_recv if nombre_interfaz in net_io else 0

            if direccion_ipv4 or direccion_mac:
                interfaces_info.append({
                    "nombre": nombre_interfaz,
                    "direccion": direccion_ipv4,
                    "mascara": subnet,
                    "mac": direccion_mac,
                    "broadcast": broadcast,
                    "bytes_enviados" : bytes_enviados,
                    "bytes_recibidos" : bytes_recibidos
                })
    \end{lstlisting}
    \item Carga del sistema durante 1, 5, 15 minutos $ \rightarrow $ \lstinline{psutil.getloadavg()}
\end{enumerate}

\subsection{Almacenamiento de datos}\label{subsec:almacenamiento-datos}

\section{Visualización Grafana}

\subsection{Carga de CPU}

La carga instantánea de la CPU se muestra en el panel de Grafana bajo la visualización llamada ``Gauge'', ya que es el tipo de visualización que mejor representa este valor en forma de medidor. Esta representación es adecuada porque permite ver de manera intuitiva el nivel de carga en tiempo real, con colores que indican el estado del sistema.\\

Está configurado para mostrar el valor exacto de la carga y una semicircunferencia que se completa en función de dicho valor. La visualización cambia de color según el porcentaje de carga de la CPU: verde representa del 0\% al 60\%, amarillo del 60\% al 90\%, y rojo del 90\% al 100\%.\\

La consulta realizada a la base de datos obtiene el valor de la carga actual de la CPU:

\begin{lstlisting}[language=SQL]
SELECT carga FROM grafana.cpu;
\end{lstlisting}

\vspace{0.4cm}
Esta consulta selecciona el campo `carga` de la tabla `cpu` en la base de datos de Grafana, lo que nos permite obtener el uso actual del procesador en un momento determinado.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\linewidth]{cargaCPU.png}
    \caption{Carga de CPU}
    \label{fig:carga-cpu}
\end{figure}

\subsection{Frecuencia de CPU}

La frecuencia instantánea de la CPU se muestra en el panel de Grafana bajo la visualización ``Gauge''. Esta visualización ha sido elegida porque representa de forma clara la frecuencia de operación del procesador en MHz, permitiendo comparar su estado actual con un valor máximo predefinido.\\

Está configurado para mostrar el valor exacto de la frecuencia y una semicircunferencia que va desde 0 hasta 4000 MHz. Se ha supuesto 4000 MHz como frecuencia máxima de la CPU debido a que la media suele ser 2000 MHz y la máquina virtual no proporcionaba un valor máximo. La visualización cambia de color según el porcentaje de la frecuencia de la CPU: verde del 0\% al 80\%, amarillo del 80\% al 90\%, y rojo del 90\% al 100\% del valor máximo de frecuencia (4000 MHz).\\

La consulta utilizada es la siguiente:

\begin{lstlisting}[language=SQL]
SELECT frecuencia FROM grafana.cpu;
\end{lstlisting}

\vspace{0.4cm}
Esta consulta selecciona el campo `frecuencia` de la tabla `cpu`, obteniendo la frecuencia actual del procesador. Esto permite a los administradores verificar si la CPU está funcionando dentro de los rangos esperados o si está alcanzando valores críticos.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\linewidth]{frecuenciaCPU.png}
    \caption{Frecuencia de CPU}
    \label{fig:frecuencia-cpu}
\end{figure}

\subsection{Uso de memoria RAM y memoria SWAP}

El uso de memoria RAM y SWAP se muestra en el panel de Grafana bajo una visualización ``Time Series'', ya que permite observar la evolución del uso de ambas a lo largo del tiempo. Este tipo de visualización permite identificar tendencias y detectar posibles picos de uso que podrían afectar el rendimiento del sistema. Además se muestran tanto los valores mínimos y máximos que se han alcanzando en ambos usos de memoria.\\

La consulta SQL utilizada para obtener estos valores es:

\begin{lstlisting}[language=SQL]
SELECT ram, swap, timestamp FROM grafana.memoria WHERE timestamp >= now() - interval 5 minute ORDER BY timestamp ASC;
\end{lstlisting}

\vspace{0.4cm}
Aquí se seleccionan los valores de `ram` y `swap`, junto con la marca de tiempo `timestamp`, filtrando solo los datos de los últimos cinco minutos y ordenándolos cronológicamente. Esto permite visualizar cómo ha cambiado el uso de la memoria en ese período de tiempo.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{memoria.png}
    \caption{Uso de memoria RAM y Swap}
    \label{fig:enter-label}
\end{figure}

\subsection{Espacio en disco}

El espacio en disco se muestra en el panel de Grafana bajo una visualización ``Pie Chart'', que permite visualizar el porcentaje de disco utilizado y el espacio libre en tiempo real. Este tipo de gráfico es elegido ya que permite ver de forma clara la proporción de almacenamiento consumido frente al disponible.\\

El gráfico está configurado para mostrar en verde el espacio utilizado y en amarillo el espacio libre. Además, en la gráfica se incluyen los porcentajes exactos con números.\\

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
SELECT espacio as "Usado", 100 - espacio as "Libre" FROM grafana.disco;
\end{lstlisting}

\vspace{0.4cm}
Esta consulta calcula dinámicamente el espacio libre restando el valor `espacio` (usado) de 100, proporcionando así un desglose claro del almacenamiento.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{disco.png}
    \caption{Espacio en Disco}
    \label{fig:enter-label}
\end{figure}

\subsection{Operaciones de E/S}

Las operaciones de entrada y salida (E/S) se muestran en el panel de Grafana con una visualización ``Time Series'' en formato de gráfico de barras. Esta elección permite ver la cantidad de operaciones de lectura y escritura en diferentes momentos del tiempo, facilitando la identificación de posibles cuellos de botella en el sistema de almacenamiento.\\

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
SELECT * FROM grafana.entradasalida WHERE timestamp >= now() - interval 5 minute ORDER BY timestamp ASC;
\end{lstlisting}

\vspace{0.4cm}
Esta consulta recupera todas las columnas de la tabla `entradasalida` para los últimos cinco minutos, lo que permite analizar la actividad reciente del disco.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{operaciones.png}
    \caption{Operaciones de E/S}
    \label{fig:enter-label}
\end{figure}

\subsection{Bytes enviados y recibidos}

Los bytes enviados y recibidos se muestran en Grafana en paneles de tipo ``Time Series'' con un gráfico de líneas. Esta representación facilita el seguimiento del tráfico de red a lo largo del tiempo. Además se muestran los últimos bytes tanto enviados como recibidos y la suma total de todos.\\

Las consultas SQL utilizadas son:

\begin{lstlisting}[language=SQL]
SELECT bytes_enviados, timestamp FROM grafana.red WHERE timestamp >= now() - interval 5 minute ORDER BY timestamp ASC;

SELECT bytes_recibidos, timestamp FROM grafana.red WHERE timestamp >= now() - interval 5 minute ORDER BY timestamp ASC;
\end{lstlisting}

\vspace{0.4cm}
Estas consultas seleccionan los bytes enviados y recibidos en los últimos cinco minutos, permitiendo detectar patrones de uso y posibles congestiones en la red.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{bytes_enviados.png}
    \caption{Bytes enviados}
    \label{fig:enter-label}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{bytes_recibidos.png}
    \caption{Bytes recibidos}
    \label{fig:enter-label}
\end{figure}

\subsection{Conexiones de red activas}

Las conexiones de red activas se muestran en Grafana en un panel de tipo ``Stat'', el cual permite visualizar las conexiones activas en un instante dado. También permite observar picos en los que hubo mayor número de conexiones de red. A medida que aumenta la cantidad de conexiones, las tonalidades de color varían.\\

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
SELECT conexiones_activas as "Conexiones Activas" FROM grafana.red;
\end{lstlisting}

\vspace{0.4cm}
Esta consulta obtiene el número de conexiones activas en tiempo real, lo que facilita la supervisión del tráfico en la red.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{conexiones_activas.png}
    \caption{Conexiones de red Activas}
    \label{fig:enter-label}
\end{figure}

\subsection{Número de procesos en ejecución}

El número de procesos en ejecución se muestra en Grafana en un panel de tipo ``Stat'', que permite visualizar la cantidad de procesos activos en un instante dado.\\

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
SELECT numero, timestamp FROM grafana.num_procesos;
\end{lstlisting}

\vspace{0.4cm}
Esta consulta proporciona el número actual de procesos, útil para detectar posibles sobrecargas en el sistema.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\linewidth]{procesos_ejecc.png}
    \caption{Procesos en ejecución}
    \label{fig:enter-label}
\end{figure}

\subsection{Lista detallada de los procesos en ejecución}

La lista de procesos en ejecución se muestra en Grafana en una tabla, lo que permite acceder, visualizar y filtrar todos los procesos en tiempo real junto con sus detalles.\\

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
SELECT usuario as Usuario, pid as PID, cpu_percent as CPU, memory_percent as MEM, vsz as VSZ, rss as RSS, estado as Stat, nombre as Nombre, ruta as Comando, hilos as hilos FROM grafana.procesos;
\end{lstlisting}

\vspace{0.4cm}
Esta consulta obtiene una lista detallada de los procesos en ejecución en el sistema como lo haria el comando \textbf{ps aux} de Linux:

\begin{itemize}
    \item \textbf{usuario}: Indica el usuario que ha iniciado el proceso.
    \item \textbf{pid}: Identificador único del proceso.
    \item \textbf{cpu\_percent}: Porcentaje de uso de CPU del proceso.
    \item \textbf{memory\_percent}: Porcentaje de uso de memoria RAM.
    \item \textbf{vsz}: Tamaño virtual del proceso en memoria.
    \item \textbf{rss}: Tamaño real del proceso en RAM.
    \item \textbf{estado}: Estado actual del proceso (ejecución, suspensión, etc.).
    \item \textbf{nombre}: Nombre del proceso.
    \item \textbf{ruta}: Ruta de ejecución del proceso o nombre del comando (en este caso coincidirá con el nombre del proceso).
    \item \textbf{hilos}: Número de hilos asociados al proceso.
\end{itemize}

Esta información es útil para el monitoreo del sistema, ya que permite detectar procesos que consumen muchos recursos y podrían estar afectando el rendimiento del sistema.

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\linewidth]{procesos.png}
    \caption{Detalles de los procesos en ejecución}
    \label{fig:enter-label}
\end{figure}

\subsection{Número de hilos totales}

El número de hilos totales de los procesos se muestra en Grafana en un panel de tipo ''Stat'', que permite visualizar el número de manera clara.\\

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
    SELECT sum(hilos) FROM grafana.procesos
\end{lstlisting}

\vspace{0.4cm}
Esta consulta consigue la suma de todos los hilos que utilizan cada proceso que se encuentra en ejecución.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\linewidth]{hilos_totales.png}
    \caption{Número de hilos totales}
    \label{fig:enter-label}
\end{figure}

\subsection{Estado de los procesos}

El estado de los procesos es mostrado en Grafana en un panel de tipo ''Pie chart'', que representa una circunferencia dividida en secciones según el porcentaje de uso de cada sección.\\

Este panel está dividido en tres partes donde se muestra el porcentaje exacto en cada momento:

\begin{itemize}
    \item Idle (inactivo) $ \rightarrow $ se refiere a los procesos que no están haciendo nada activamente, pero tampoco están bloqueados o esperando algo específico. Se representa en color azul.
    \item Running (ejecutándose) $ \rightarrow $ se refiere a los procesos que están en ejecución en la CPU o listos para ejecutarse. Se representa en color amarillo.
    \item Sleeping (durmiendo) $ \rightarrow $ se refiere a los procesos que se encuentran esperando un evento o recurso antes de continuar con su ejecución. Se representa en color rosa.
\end{itemize}

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
SELECT 
    COUNT(CASE WHEN estado = 'Idle' THEN 1 END) AS Idle,
    COUNT(CASE WHEN estado = 'Running' THEN 1 END) AS Running,
    COUNT(CASE WHEN estado = 'Sleeping' THEN 1 END) AS Sleeping
FROM grafana.procesos;
\end{lstlisting}


\vspace{0.4cm}
Esta consulta cuenta las veces que se repite cada estado de cada proceso. Por ejemplo, si el estado es 'Idle' devuelve un 1, sino es NULL.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{estado_procesos.png}
    \caption{Estado de los procesos}
    \label{fig:enter-label}
\end{figure}

\subsection{Número de paquetes enviados/recibidos}

El número de paquetes enviados y recibidos se muestra en Grafana en un panel de tipo ''Gauge'', que muestra tanto el número inmediato como una semicircunferencia comparando las dos medidas capturadas.\\

La consulta SQL utilizada es:

\begin{lstlisting}[language=SQL]
SELECT paquetes_enviados as Enviados, paquetes_recibidos as Recibidos FROM grafana.red
\end{lstlisting}

\vspace{0.4cm}
Esta consulta muestra el número de paquetes enviados y recibidos que se almacena en la tabla de la red.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{paquetes_enviados_recibidos.png}
    \caption{Paquetes enviados y recibidos}
    \label{fig:enter-label}
\end{figure}

\subsection{Tiempo activo}

El tiempo activo del servidor se muestra en Grafana en un panel de tipo ''Table'', en donde se puede ver que esta distribuido en días, horas, minutos y segundos.\\

La consulta SQL que se ha utilizado es:

\begin{lstlisting}[language=SQL]
SELECT dias, horas, minutos, segundos FROM grafana.tiempo_activo ORDER BY timestamp DESC LIMIT 1
\end{lstlisting}

\vspace{0.4cm}
Esta consulta muestra el último número de días, horas, minutos y segundos almacenados en la tabla de tiempo\_activo.

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\linewidth]{tiempo_activo.png}
    \caption{Tiempo activo}
    \label{fig:enter-label}
\end{figure}

\subsection{Interfaces de red}

La información de las interfaces de red se muestra en Grafana en un panel de tipo ''Table'', permitiendo visualizar y filtrar claramente toda la información recopilada de las distintas interfaces de red.\\

La consulta SQL que se ha utilizado es:

\begin{lstlisting}[language=SQL]
SELECT nombre as Nombre, direccion as Dirección, mascara as Máscara, mac as Mac FROM grafana.interfaces 
\end{lstlisting}

\vspace{0.4cm}
Esta consulta muestra la siguiente información de las interfaces de red:

\begin{itemize}
    \item Nombre: es el nombre que se asigna a una interfaz de red.
    \item Dirección IP: es el identificador único asignado a una interfaz de red dentro de una red.
    \item Máscara de red: determina qué parte de la dirección IP identifica a la red y qué parte identifica el host.
    \item Dirección MAC: es el identificador físico de la tarjeta de red.
    \item BytesEnviados: es el número de bytes enviados por la interfaz.
    \item BytesRecibidos: es el número de bytes recibido por la interfaz.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\linewidth]{InterfacesRed.jpg}
    \caption{Información de las interfaces de red}
    \label{fig:enter-label}
\end{figure}

\subsection{Carga del sistema}

La carga del sistema se muestra en Grafana en un panel de tipo ''Gauge'', ya que este panel es perfecto para visualizar el valor inmediato de la carga y una semicircunferencia indicando el estado de la carga.\\

Nuestro sistema tiene dos núcleos, por lo que el valor que se representa en nuestra semicircunferencia llega hasta cuatro. Si la carga se encuentra entre el 0 y el 2, quiere decir que es una carga normal (verde), si se encuentra entre el 2 y el 3'2, quiere decir que es una carga media (amarillo), y si supera los 3'2 hasta 4, quiere decir que la carga es alta (rojo).\\

La consulta SQL que se ha utilizado es:

\begin{lstlisting}[language=SQL]
SELECT carga1 as 1minuto, carga5 as 5minutos, carga15 as 15minutos FROM grafana.sistema
\end{lstlisting}

\vspace{0.4cm}
Esta consulta muestra el valor inmediato de la carga del sistema en 1, 5 y 15 minutos.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{carga_sistema.png}
    \caption{Carga del sistema}
    \label{fig:enter-label}
\end{figure}

\section{Distribución de trabajo y organización}

\section{Bibliografía}

\printbibliography


\end{document}